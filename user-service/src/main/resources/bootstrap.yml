server:
  port: 9001

spring:
  profiles:
    active: local
  application:
    name: user-service

  cloud:
    config:
      enabled: true
      uri:
        - http://localhost:8888
      retry:
        maxAttempts: 10