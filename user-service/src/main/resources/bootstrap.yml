spring:
  profiles:
    active: local
  application:
    name: user-service

  cloud:
    config:
      enabled: true
      uri: http://localhost:8888
      failFast: true
      retry:
        maxAttempts: 10