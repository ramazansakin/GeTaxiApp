server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ramazansakin/GeTaxiApp-Config-Repo
          clone-on-start: true

management:
  endpoints:
    web:
      exposure:
        include: '*'

health:
  config:
    enabled: false
