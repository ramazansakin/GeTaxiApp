server:
  port: 8888

spring:
  application:
    name: CONFIG-SERVER
  profiles:
    active: local
  cloud:
    config:
      server:
        git:
          clone-on-start: true
          uri: https://github.com/ramazansakin/GeTaxiApp-Config-Repo
